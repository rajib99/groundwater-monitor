server {
    listen 80;
    server_name _;

    # WebSocket live feed — must come before the /api/ block
    location /ws/ {
        set $backend_up http://backend:8000;
        proxy_pass         $backend_up;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
        proxy_set_header   X-Real-IP  $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # Report downloads (longer timeout, lower rate limit — must come before /api/)
    location ~ ^/api/sites/\d+/report/ {
        limit_req  zone=report burst=3 nodelay;

        set $backend_up http://backend:8000;
        proxy_pass         $backend_up;
        proxy_http_version 1.1;
        proxy_set_header   Connection         "";
        proxy_set_header   Host               $host;
        proxy_set_header   X-Real-IP          $remote_addr;
        proxy_set_header   X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto  $scheme;
        proxy_read_timeout 300s;
    }

    # REST API
    location /api/ {
        limit_req  zone=api burst=60 nodelay;
        limit_conn conn 20;

        set $backend_up http://backend:8000;
        proxy_pass         $backend_up;
        proxy_http_version 1.1;
        proxy_set_header   Connection         "";
        proxy_set_header   Host               $host;
        proxy_set_header   X-Real-IP          $remote_addr;
        proxy_set_header   X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto  $scheme;
        proxy_read_timeout 120s;
    }

    # Everything else → Next.js frontend
    location / {
        set $frontend_up http://frontend:3000;
        proxy_pass         $frontend_up;
        proxy_http_version 1.1;
        proxy_set_header   Connection         "";
        proxy_set_header   Host               $host;
        proxy_set_header   X-Real-IP          $remote_addr;
        proxy_set_header   X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto  $scheme;
        proxy_read_timeout 60s;
    }
}
